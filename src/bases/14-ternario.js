let firstName;
let lastName = 'Herrera';

// El operador || hace la siguiente logica: si el first name no tiene valor, entonces, ponme 'No first name'
//console.log(`${firstName || 'No fist Name'} ${lastName || 'No last name'}`);

//Operador ternanio
const isActive = true;
const message = isActive ? 'Esta activo' : 'Está inactivo';
console.log(message);
